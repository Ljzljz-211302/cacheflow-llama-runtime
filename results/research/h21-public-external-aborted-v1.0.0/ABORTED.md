# H21 v1.0 aborted run

This run completed request collection but was stopped before producing a summary or manifest. Every forced-Paged trace cell recorded zero Paged route entries. The public cold prompts had no resident prefix when the one-shot KV action decision was made, so the request selected recompute; the same pending field was then cleared before later generated decode steps.

The raw files are retained only as diagnosis evidence. They are not a formal performance result and are excluded from `verify.ps1`. Protocol v1.1 separated persistent request decode routing from the one-shot action observation; v1.2 additionally made Direct and Paged use identical prefill/decode phase boundaries.
